document.addEventListener('DOMContentLoaded', () => {
    const container = document.getElementById('envelopeContainer');
    const envelope = document.getElementById('mainEnvelope');
    const openBtn = document.getElementById('openBtn');

    // ฟังก์ชันสั่งเปิด/ปิด
    function handleOpen(e) {
        if (e) e.stopPropagation();
        
        const isOpening = !container.classList.contains('open');
        container.classList.toggle('open');

        if (isOpening) {
            // เปลี่ยนข้อความบนปุ่ม
            openBtn.innerText = "🌸 กดอีกครั้งเพื่อเก็บจดหมาย 🌸";

            // หาพิกัดปล่อยหัวใจ
            const rect = envelope.getBoundingClientRect();
            const centerX = rect.left + rect.width / 2;
            const centerY = rect.top + rect.height / 2;

            createHeartBurst(centerX, centerY);
        } else {
            openBtn.innerText = "🌸 กดตรงนี้เพื่อเปิดอ่านนะ 🌸";
        }
    }

    // ผูกเหตุการณ์ทั้งคลิกซอง และคลิกปุ่ม
    envelope.addEventListener('click', handleOpen);
    openBtn.addEventListener('click', handleOpen);

    // สร้างหัวใจพุ่งกระจาย
    function createHeartBurst(x, y) {
        const heartEmojis = ['❤️', '💖', '💗', '💕', '🌸', '✨'];
        const numHearts = 18;

        for (let i = 0; i < numHearts; i++) {
            const heart = document.createElement('div');
            heart.classList.add('heart-particle');
            heart.innerText = heartEmojis[Math.floor(Math.random() * heartEmojis.length)];

            const angle = (i / numHearts) * 360 + (Math.random() * 20 - 10);
            const distance = Math.random() * 120 + 60;
            const tx = Math.cos(angle * Math.PI / 180) * distance + 'px';
            const ty = Math.sin(angle * Math.PI / 180) * distance + 'px';
            const rot = (Math.random() * 360) + 'deg';

            heart.style.left = x + 'px';
            heart.style.top = y + 'px';
            heart.style.setProperty('--tx', tx);
            heart.style.setProperty('--ty', ty);
            heart.style.setProperty('--rot', rot);

            document.body.appendChild(heart);

            setTimeout(() => {
                heart.remove();
            }, 1200);
        }
    }

    // สร้างซากุระร่วง
    const sakuraContainer = document.getElementById('sakura-container');
    const sakuraEmojis = ['🌸', '🌺', '✨', '🌸', '💖'];
    for (let i = 0; i < 20; i++) {
        const petal = document.createElement('div');
        petal.classList.add('sakura');
        petal.innerText = sakuraEmojis[Math.floor(Math.random() * sakuraEmojis.length)];
        petal.style.left = Math.random() * 100 + 'vw';
        petal.style.animationDuration = (Math.random() * 5 + 5) + 's';
        petal.style.animationDelay = (Math.random() * 5) + 's';
        petal.style.fontSize = (Math.random() * 0.8 + 1) + 'rem';
        sakuraContainer.appendChild(petal);
    }
});
document.addEventListener('DOMContentLoaded', () => {
    const checkbox = document.getElementById('toggle-letter');
    const label = document.getElementById('letterLabel');

    // สร้างระบบเสียงสังเคราะห์ในตัวบราวเซอร์ (ชัวร์ที่สุดสำหรับ Safari/iPad)
    let audioCtx = null;

    function playChime() {
        try {
            // สร้าง AudioContext เมื่อมีการกดสัมผัส
            if (!audioCtx) {
                const AudioContext = window.AudioContext || window.webkitAudioContext;
                audioCtx = new AudioContext();
            }

            if (audioCtx.state === 'suspended') {
                audioCtx.resume();
            }

            // ตัวสร้างคลื่นเสียง "ติ๊ง" ✨ (ความถี่สูง ใสๆ)
            const osc = audioCtx.createOscillator();
            const gain = audioCtx.createGain();

            osc.type = 'sine';
            
            // เล่นโน้ตเสียงป๊อป/ติ๊งใสๆ (ไล่เสียงสูง)
            const now = audioCtx.currentTime;
            osc.frequency.setValueAtTime(880, now); // โน้ต A5
            osc.frequency.exponentialRampToValueAtTime(1760, now + 0.1); // พุ่งไป A6

            // ปรับความดังให้ค่อยๆ จางหายไปแบบนุ่มนวล
            gain.gain.setValueAtTime(0.3, now);
            gain.gain.exponentialRampToValueAtTime(0.001, now + 0.35);

            osc.connect(gain);
            gain.connect(audioCtx.destination);

            osc.start(now);
            osc.stop(now + 0.35);
        } catch (e) {
            console.log("Audio Error:", e);
        }
    }

    // ดักจับการกดทั้งแบบ Touch (มือถือ/iPad) และ Click (คอมพิวเตอร์)
    function handleSound(e) {
        // เล่นเสียงเฉพาะตอนที่กำลังจะ "เปิด" ซองจดหมาย
        if (!checkbox.checked) {
            playChime();
        }
    }

    label.addEventListener('touchstart', handleSound, { passive: true });
    label.addEventListener('click', handleSound);
});
